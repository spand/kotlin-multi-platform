apply plugin: 'kotlin2js'

repositories {
    mavenCentral()
    maven {
        url "$kotlinEapRepo"
    }
    maven {
        url "https://dl.bintray.com/kotlin/kotlin-eap"
    }
}

dependencies {
    compile project(':foo:js')
    compile "org.jetbrains.kotlin:kotlin-stdlib-js:$kotlinVersion"
}
