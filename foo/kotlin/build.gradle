apply plugin: 'kotlin'

repositories {
    mavenCentral()
    maven {
        url "$kotlinEapRepo"
    }
    maven {
        url "https://dl.bintray.com/kotlin/kotlin-eap"
    }
}

dependencies {
    compile project(':foo:jvm')
    compile "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlinVersion"
}
